[project]
name = "blog"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mkdocs-blog",
    "mkdocs-material>=9.6.22",
]

[tool.uv.sources]
mkdocs-blog = { git = "https://github.com/andyoakley/mkdocs-blog" }
